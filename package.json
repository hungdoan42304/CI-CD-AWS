{
  "name": "sample-ci-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "CI/CD lab with GitHub, Jenkins and EC2",
  "scripts": {
    "start": "node app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
